class WeekForecast {
  const WeekForecast(this.label, this.minTemp, this.maxTemp, this.weatherType);

  final String label;
  final double minTemp;
  final double maxTemp;
  final String weatherType;
}
